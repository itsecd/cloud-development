AWSTemplateFormatVersion: '2010-09-09'
Description: 'Cloud formation template for patient project (SQS + Minio)'

Parameters:
  QueueName:
    Type: String
    Description: Name for the SQS queue
    Default: 'patient-queue'

Resources:
  PatientQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Ref QueueName
      VisibilityTimeout: 30
      MessageRetentionPeriod: 345600
      DelaySeconds: 0
      ReceiveMessageWaitTimeSeconds: 0
      Tags:
        - Key: Name
          Value: !Ref QueueName
        - Key: Environment
          Value: Sample

Outputs:
  SQSQueueName:
    Description: Name of the SQS queue
    Value: !GetAtt PatientQueue.QueueName

  SQSQueueArn:
    Description: ARN of the SQS queue
    Value: !GetAtt PatientQueue.Arn
